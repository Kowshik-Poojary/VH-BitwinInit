repos:
  - repo: local
    hooks:
      - id: leakguard
        name: LeakGuard (resource leak scan)
        entry: leakguard scan --fail-on error --fix
        language: system
        types: [python]
        pass_filenames: true
